<?php
/**
 * Vista de detalle del paso 5 — Carta de Examinadores (staff)
 * Variables: $proceso, $cicloActual, $evidencias, $carta
 */
$this->headTitle('Carta de Examinadores — Proceso ' . (int) ($proceso['cod_proceso'] ?? 0));

$nombreEstudiante = trim(($proceso['nombres'] ?? '') . ' ' . ($proceso['apellidos'] ?? ''));
?>

<div class="col-lg-12">

    <ol class="breadcrumb">
        <li><a href="<?= $this->url('home') ?>"><i class="fa fa-home"></i> Inicio</a></li>
        <li><a href="<?= $this->url('examen', ['action' => 'carta-examinadores']) ?>">Carta de Examinadores</a></li>
        <li class="active"><?= $this->escapeHtml($nombreEstudiante ?: 'Proceso ' . (int) ($proceso['cod_proceso'] ?? 0)) ?></li>
    </ol>

    <!-- Datos del estudiante -->
    <div class="panel panel-default" style="margin-bottom: 14px;">
        <div class="panel-body" style="padding: 12px 20px;">
            <div class="row">
                <div class="col-sm-6">
                    <strong><i class="fa fa-user"></i> Estudiante:</strong>
                    <?= $this->escapeHtml($nombreEstudiante ?: '-') ?>
                </div>
                <div class="col-sm-3">
                    <strong>Carné:</strong>
                    <?= $this->escapeHtml($proceso['registro_academico'] ?? $proceso['carne'] ?? '-') ?>
                </div>
                <div class="col-sm-3">
                    <strong>Tipo de examen:</strong>
                    <?= $this->escapeHtml($proceso['tipo_examen'] ?? '-') ?>
                </div>
            </div>
        </div>
    </div>

    <!-- Partial reutilizable con la lógica de staff del paso 5 -->
    <?= $this->partial('eep/examen/partial/paso5-carta-examinadores', [
        'proceso'     => $proceso,
        'cicloActual' => $cicloActual,
        'evidencias'  => $evidencias,
        'carta'       => $carta,
    ]) ?>

</div><!-- /col-lg-12 -->
